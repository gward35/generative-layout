{
  "name": "generative",
  "version": "1.0.0",
  "description": "Generative experiment",
  "main": "server.js",
  "dependencies": {
    "canvas": "^2.6.0",
    "concurrently": "^4.1.2",
    "express": "^4.17.1",
    "node-sass": "^4.12.0",
    "nodemon": "^1.19.1",
    "p5": "^0.9.0",
    "random-seed": "^0.3.0"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "node-sass --include-path scss public/sass/styles.scss public/css/styles.css",
    "watch-css": "nodemon -e scss -x \"npm run build-css\"",
    "watch": "browser-sync start --server --index browser-sync.html --watch server.js src/*",
    "start": "concurrently \"nodemon server.js\" \"npm run watch\""
  },
  "author": "",
  "license": "ISC"
}
